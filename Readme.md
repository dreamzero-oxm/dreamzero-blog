# WebTemplate-Go -Gin

一款开发医疗大模型系统的后端服务。

## 部署

### 编译并生成二进制文件

```bash
make build
```

- ./mlm-server 运行
- 也可以直接用go run ./main.go 运行

### 版本查看
- `http://127.0.0.1:8080/version` 默认是这个，通过修改配置文件可以更改port
- `./mlm-server -v`

